<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".DatabasePossibilities" >

    <Button
        android:id="@+id/exportToCSV"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:onClick="exportToCSV"
        android:text="Export do CSV" />
    <Button
        android:id="@+id/exportToXML"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/exportToCSV"
        android:onClick="exportToXML"
        android:text="Export do XML" />
    <Button
        android:id="@+id/exportToSQL"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/exportToXML"
        android:onClick="exportToSQL"
        android:text="Export do SQL" />
    <Button
        android:id="@+id/deleteDatabase"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/exportToSQL"
        android:onClick="deleteDatabase"
        android:text="Vymaž databázi" />

</RelativeLayout>